import React from 'react';

import { Button } from '../../packages'
import { Styled } from './styled';


export const StartGame = () => {
  return (
    <Styled.StartGame>
      <Button>
        Start game
      </Button>
    </Styled.StartGame>
  );
};
