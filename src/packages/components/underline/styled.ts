import styled from 'styled-components';

const Underline = styled.div`
  position: absolute;
  width: 0;
  height: 1px;
  bottom: 0px;
  transition: 0.1s linear;
  cursor: pointer;
`;

export const Styled = {
  Underline,
};
